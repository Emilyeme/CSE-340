<%# Unit 4, Select Inv Item activity %>
<% if (title) {%>
<h1><%= title %></h1>
<%} else {res.redirect('/')} %>

<% if (messages && messages.error) { %>
  <div class="flash-message error">
    <%= messages.error %>
  </div>
<% } %>

<% if (messages && messages.success) { %>
  <div class="flash-message success">
    <%= messages.success %>
  </div>
<% } %>
<% if (messages && messages.notice) { %>
  <div class="notice">
    <%= messages.notice %>
  </div>
<% } %>

<ul class="boldList">
  <a href="/inv/add-classification" title="Add a new classification">Add New Classification</a>
</p>
<p>
  <a href="/inv/add-inventory" title="Add a new vehicle">Add New Vehicle</a>
</p>
</ul>

<hr>
<h2>Manage Inventory</h2>
<p>Select a classification from the list to see the items belonging to the classification.</p>

<%- classificationSelect %>

<table id="inventoryDisplay"></table>
<noscript>JavaScript must be enabled to use this page.</noscript>

<script src="../../js/inventory.js"></script>